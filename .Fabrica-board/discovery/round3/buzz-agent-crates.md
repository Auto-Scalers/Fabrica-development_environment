# Buzz Agent-Surface Crates — Discovery Report (Round 3, P5)

Root: `_sources/buzz/crates/` (read-only). Scope: buzz-acp, buzz-agent, buzz-cli, buzz-workflow.
All paths relative to `_sources/buzz/crates/`. Line refs verified by direct reads.

---

## 1. buzz-acp — ACP harness bridging Buzz events to AI agents (~40K lines)

Files: `buzz-acp/src/{lib.rs (8702 ln), relay.rs (6304), pool.rs (9302), queue.rs (5394), acp.rs (5030), filter.rs (787), config.rs, setup_mode.rs, usage.rs, observer.rs, engram_fetch.rs, pool_lifecycle.rs, main.rs}`. `main.rs` is a 4-line shim: `fn main() { buzz_acp::run() }` — all logic in `lib.rs`.

### 1.1 relay.rs — connection + NIP-42 auth
- **Constants** (`relay.rs:29-115`): event channel cap 256 (env-tunable via `event_channel_capacity()` :35), cmd channel 64, seen-ID dedup limit 12_000 (:44), ping every 30s / pong timeout 10s (:47-50), WS send timeout 10s (:53), stable-connection threshold 60s (:57), since-skew 5s (:59), AUTH timeout 20s (:64), connect timeout 30s (:69), startup backoff array of 5 durations (:81), DNS retry 2s (:97), request pacing 125ms (:102), drain budget 1 cmd/iter (:107), gated observer queue cap 256 (:115).
- **`HarnessRelay` struct** (`relay.rs:565-584`): `event_rx` mpsc receiver of `Option<BuzzEvent>`, optional `observer_control_rx`, `cmd_tx` for `RelayCommand`s, reqwest HTTP client for the HTTP bridge, keys for NIP-42 signing + NIP-98 HTTP auth, optional NIP-OA `auth_tag`, background task JoinHandle.
- **`connect()`** (`relay.rs:627-664`): calls `retry_initial_connect(do_connect)` (:638) — bounded jittered backoff on transient failures; terminal errors (bad URL/auth tag/rejected key) fail immediately via `is_terminal_connect_error` (:3688). Spawns `run_background_task` with the authenticated socket + handshake buffer.
- **`do_connect()`** (`relay.rs:3856-3893`): parse URL → `connect_async` under CONNECT_TIMEOUT → `wait_for_auth_challenge` (AUTH_TIMEOUT) → `send_auth_response` → `wait_for_any_ok`; non-accepted OK ⇒ `RelayError::AuthFailed`.
- **`wait_for_auth_challenge()`** (`relay.rs:3896-3945`): checks buffer first, then loops reading frames until deadline; buffers any non-AUTH messages (so nothing is lost pre-auth); answers WS pings.
- **`send_auth_response()`** (`relay.rs:3460-3494`): builds kind:22242 Authentication event. With a NIP-OA `auth_tag` it cannot use the `EventBuilder::auth()` shortcut and manually assembles tags `[relay, url]`, `[challenge, c]` + the attestation tag; otherwise standard `EventBuilder::auth(challenge, relay_url)`. Sends `["AUTH", event]` JSON frame.
- **Per-channel access denial handling** (`relay.rs:3521-3559`): exact-match CLOSED reasons `"restricted: not a channel member"` / `"restricted: channel access revoked"` drop only that channel's subscription and keep the socket; everything else falls through to reconnect. Comment warns never to prefix-match.
- **Reconnect machinery**: `run_background_task` (:1565), `handle_ws_message` (:2074), `resubscribe_after_reconnect` (:2520), `try_autonomous_reconnect` (:2924), `wait_for_reconnect` (:3053), `drain_commands` (:2824), `drain_rate_limited_pending` (:2698), `jittered_duration` (:3368).
- **`RestClient`** (`relay.rs:251-459`): HTTP bridge client; ws→http URL conversion `relay_ws_to_http` (:3501); retriable-status check (:260) + 3-step retry delays (:266).

### 1.2 queue.rs — per-channel batching + dedup
- **Constants** (`queue.rs:24-46`): MAX_PENDING_PER_CHANNEL=500, MAX_BATCH_EVENTS=50, base retry delay 5s → max 300s exponential backoff, in-flight deadline buffer 100s, default in-flight deadline 7300s.
- **`EventQueue` struct** (`queue.rs:137-171`): per-channel `VecDeque<QueuedEvent>`; `in_flight_channels` set + per-channel deadlines/batch sizes; `retry_after`/`retry_counts` for backoff/dead-lettering; `dedup_mode`; `cancelled_batches` + `cancel_reasons` (steer vs interrupt) merged into next FlushBatch as "[Previous request — interrupted]" sections; `withheld_native_steer` — events parked while a goose-native steer is in flight, restored to queue front preserving original `received_at` fairness; `in_flight_deadline` must exceed max_turn_duration.
- **`push()`** (`queue.rs:230-252`): Drop-mode dedup silently discards events for in-flight channels; enforces depth cap by dropping oldest.
- **`flush_next()`** (`queue.rs:260-356+`): auto-expires stuck in-flight channels past deadline (logs "BUG: ... orphaned", recovers withheld steer events); picks channel whose head event has oldest `received_at` (FIFO fairness across channels, excluding in-flight/throttled); falls back to flushing cancelled batches when no new work; drains up to MAX_BATCH_EVENTS and **stable-sorts by created_at** because relay replay delivers newest-first but prompt formatting requires last=newest (:346-350).
- **Prompt composition**: `ThreadTags`/`parse_thread_tags` (:867-887), slash-command extraction (:940, :999), `ConversationContext`/`ContextMessage` (:1008-1036), label sanitization (max 64 chars, :1066-1103), reply-instruction appenders (:1192-1207), human-facing turn detection (:1225), reply-anchor resolution (:1252), channel description append (max 500 chars, :1274-1318), context hints (:1318), `format_prompt(batch, FormatPromptArgs)` (:1568) producing the final prompt strings; `MergeFraming` (:1719) for merged batch framing.

### 1.3 pool.rs — agent pool claim/return lifecycle
- **Module doc** (`pool.rs:13-20`): lifecycle is `try_claim() → OwnedAgent (removed from slot)` … `return_agent(agent) → back in slot`. `AcpClient` is NOT Clone — ownership moves out on claim and back on return.
- **`AgentPool` struct** (`pool.rs:295`): `agents: Vec<Option<OwnedAgent>>` slots (positions preserved so `agent.index` matches slot even after failed startups — `from_slots` :653), `result_tx/result_rx` unbounded mpsc for prompt results, `JoinSet` + `task_map: HashMap<tokio::task::Id, TaskMeta>`.
- **`try_claim(channel_id)`** (`pool.rs:670-686`): two passes — Pass 1 prefers an idle agent that already has an ACP session for the channel (session affinity); Pass 2 takes first idle slot. Returns None if all checked out.
- **`return_agent()`** (`pool.rs:689-702`): puts agent back at its fixed slot index; double-return logs ERROR "BUG: slot already occupied" then overwrites (leak-avoidance over strictness).
- **Support API**: `any_idle` (:705), `has_session_for` (affinity_hit metric, :711), `live_count` = idle + checked-out, used to detect total agent death for respawn (:722).
- **Steering**: `send_steer()` (:758-774) routes a goose-native `SteerRequest` into the in-flight task's capacity-1 mpsc; `Err(SteerError::PromptCompleted)` if no in-flight task (soft no-op). Caller owns ack oneshot and must call `queue.mark_native_steer_pending(...)` before spawning the watcher to close result-vs-ack race.
- **`run_prompt_task()`** (`pool.rs:1769`): the per-turn async task; guards: `ReactionGuard` (:4117, 👀 reaction on receipt, 💬 while working, cleared after; concurrency 10, :4665-4683), `LivenessGuard` (:4234), `TurnCompletionGuard` (:4273); turn liveness watchdog `run_turn_liveness` (:4168); publishes agent-turn metrics (:4391); requeues cancelled batches (`requeue_batch_if_queue` :3972, `requeue_cancelled_batch` :3985); classifies control-cancel failures (:4006-4059).
- **Model/session setup helpers**: `create_session_and_apply_model` (:1008), `apply_model_switch` (:1319, MODEL_SWITCH_TIMEOUT 5s), `apply_startup_effort` (:1415), `apply_permission_mode` (:1533, 5s timeout), MCP server construction incl. git-origin env (:1268), system-prompt framing (`framed_system_prompt` :1626, workspace/team/huddle/canvas sections :1654-1739).
- **Context fetchers** (REST): thread context w/ retries (:3504-3638), DM context (:3671), profile lookups (:3412-3504), conversation-context delta (:3255); timeouts CONTEXT_FETCH_TIMEOUT 3s / COUNT_TIMEOUT 500ms (:930-936).

### 1.4 acp.rs — stdio JSON-RPC ACP client + timeouts
- **`AcpClient` struct** (`acp.rs:141-217`): child process, stdin writer, stdout `FramedRead<_, LinesCodec>` bounded at MAX_LINE_SIZE=10MB (`acp.rs:23`) to prevent OOM from rogue agents; monotonic JSON-RPC id counter; pending-permission id + responded guard (double-response protection); `last_prompt_id` + `current_hard_deadline` shared between prompt and cancel-drain so cancel doesn't start a fresh timer ("prevents double-jeopardy", :163-170); observer handle/context; `active_run_id` from `_meta.goose.activeRunId` session_info_update notifications (required `expectedRunId` for `_goose/unstable/session/steer`, :177-191); `steering_supported` gate set ONLY from initialize `_meta.steering.supported` — comment explains error-code probing is forbidden because codex-acp answers unknown methods with `{}` success (:192-202); per-turn steer_rx channel; two usage trackers (goose cumulative + standard per-turn) with adapter identity.
- **`spawn()`** (`acp.rs:454-567`): piped stdin/stdout, inherited stderr, `kill_on_drop(true)`; env layering order: (1) per-runtime defaults from `config::default_agent_env(command)` if not in parent env (:500-504), (2) persona `extra_env` operator-wins (skip if already set in parent, :506-514), (3) special CODEX_CONFIG deep-merge path — persona base + generated overlay + parent-env precedence + forced `sandbox_workspace_write.network_access=true` applied LAST so relay access is guaranteed (:244-260, `deep_merge` :222); Unix `process_group(0)` so SIGKILL doesn't hit harness group (:522); Windows `configure_no_window` (:2341, def :527); adapter identity normalization → StandardAdapterKind Claude/Codex (:529-536).
- **`initialize()`** (`acp.rs:611`): pins protocolVersion 2 ("squatting ahead of upstream ACP RFD"); advertises clientCapabilities auth.terminal + goose customNotifications (tests :2500-2521).
- **Timeouts**: `session_prompt_with_idle_timeout` (:755) / `session_prompt_blocks_with_idle_timeout` (:777) — sets hard_deadline = now + max_duration, delegates to read loop with idle_timeout + hard deadline; on some errors deliberately LEAVES current_hard_deadline set so cancel_with_cleanup shares the budget (:822-835). `cancel_with_cleanup` (:963+) uses stored deadline or 30s fallback; 5s grace for child.wait on shutdown (:439). Steer methods: GOOSE_STEER_METHOD `_goose/unstable/session/steer` (:364), ACP_STEER_METHOD `_session/steering` (:370), outcomes injected/startedNewTurn (:374-380). Permission responses allow_once/cancelled (:2106-2115). `Drop` impl (:2297) + `kill_process_group` (:2323 unix / :2334 stub).
- **Session/new response parsing**: `extract_model_config_options` (:2166), `extract_model_state` (:2181), `resolve_model_switch_method` (:2213), `model_in_catalog` (:2266), SystemPromptTransport enum (:2141).

### 1.5 filter.rs — evalexpr subscription rules
- **`SubscriptionRule`** (`filter.rs:83-114`): channels scope (`ChannelScope` :56), kinds list, require_mention, optional evalexpr `filter` string + pre-compiled AST (`Arc<evalexpr::Node>`), atomic `consecutive_timeouts` counter.
- **Eval bounds** (`filter.rs:158-175`): max expression length caps worst-case O(2^n) paths; EVAL hard wall-clock timeout; semaphore bounding concurrent `spawn_blocking` evals; evaluation runs on blocking thread with `eval_boolean_with_context` on the pre-compiled node (:197-243).
- **`build_eval_context()`** (`filter.rs:264-334`): exposes vars `content`, `author` (hex pubkey), `kind` (int), `channel_id` (UUID str), `timestamp` (int); registers custom fns `str_contains`, `str_starts_with`, `str_ends_with`, `str_len` — intentionally duplicated from buzz-workflow so this crate has no runtime dep on it.
- **`match_event()`** (`filter.rs:368-448`): ordered rules, first match wins. Per rule: (1) channel scope, (2) kinds wildcard/list, (3) mention = `p` tag equal to agent pubkey via `tag.as_slice()`, (4) evalexpr filter. **Fail-closed semantics**: ANY eval error or timeout returns None for the ENTIRE call (never falls through to next rule — would silently widen subscription); after MAX_CONSECUTIVE_TIMEOUTS=5 consecutive timeouts a rule is disabled (ERROR log, return None) (:336-367, :401-447).

### 1.6 lib.rs — main event loop + heartbeat
- **Entry**: `run()` (:1879) → `tokio_main()` (:1885). Main select loop ~:2200-3400 arms: relay events, heartbeat timer, presence heartbeat timer, control events, shutdown.
- **Heartbeat**: interval timer built from `config.heartbeat_interval_secs` (:2219-2228); `heartbeat_in_flight` flag prevents overlap; skips logged when pool not ready / events pending / busy (:3059-3077); presence heartbeat sends presence pings and is aborted before offline-sent on shutdown (:3082-3096, :3492). **`dispatch_heartbeat()`** (`lib.rs:4379-4429`): claims agent via `pool.try_claim(None)`, composes prompt from `ctx.heartbeat_prompt` or `default_heartbeat_prompt()` (:4484), spawns `pool::run_prompt_task` in JoinSet, registers TaskMeta with channel_id=None, sets flag. Flag cleared on PromptSource::Heartbeat completion (:4014) and force-cleared for panicked agents ("cleared wedged heartbeat_in_flight", :4286-4287).
- **Owner resolution & security**: `resolve_agent_owner` (:123) — priority BUZZ_AUTH_TAG env (NIP-OA attestation, verified, :124-135) → --agent-owner flag (:2013); without owner, non-owner messages are dropped unless --respond-to=anyone (:2013-2026). Sibling-agent checks via profiles (:192, :291). Owner control commands (`!rotate`, `!cancel` etc.) require kind + content + agent mention (`is_owner_control_command` :3536, tests :5134+).
- **Control plane**: native steer attempt (`try_native_steer` :3621), cancel-turn control (:1277), switch-model control (:1322), mode-gate signals (:3557), project-owner announcements (:1150-1275), observer publisher with size-budgeted event shrinking (elision markers, :892-1016).
- **Crash recovery**: `recover_panicked_agent` (:4244), respawn tasks with SlotCircuit crash history (`spawn_respawn_task` :4510, `any_respawn_in_flight` :1531), inactivity/idle-pool sleep checks (:1626-1645), graceful `shutdown_agent_pool` (:4580).
- **Env injection into agent subprocesses — `build_mcp_servers()`** (`lib.rs:5022-5077`): builds one McpServer from `config.mcp_command` with env: `BUZZ_RELAY_URL` = config.relay_url; `BUZZ_PRIVATE_KEY` = secret key bech32 (expect() justified in comment: bogus secret would cause delayed downstream failures); `BUZZ_AUTH_TAG` forwarded from process env only when set AND non-empty (tests :6803-6839 pin both behaviors); `BUZZ_ACP_DISPLAY_NAME` pass-through for dev-mcp git author name. These same EnvVars are declared in session/new `mcpServers[].env` so adapters inject them into agent processes (schema test acp.rs:2524-2556). Config side: clap args `--relay-url` env BUZZ_RELAY_URL default ws://localhost:3000, `--private-key` env BUZZ_PRIVATE_KEY hidden values (`config.rs:246-249`); legacy alias migration BUZZ_ACP_PRIVATE_KEY→BUZZ_PRIVATE_KEY (`config.rs:850`). Base prompt teaches agents the three env vars + exit codes (`base_prompt.md:11,30`). Setup mode parses BUZZ_AUTH_TAG for NIP-OA membership (`setup_mode.rs:318-319`).

---

## 2. buzz-agent — minimal ACP-compliant agent (~27K lines)

Files: `buzz-agent/src/{lib.rs (1039 ln, JSON-RPC server), agent.rs (session loop), llm.rs (7903), mcp.rs (1204), handoff.rs, model_capabilities.rs, auth.rs, config.rs, catalog.rs, builtin.rs, hints.rs, types.rs, wire.rs, main.rs}`.

### 2.1 llm.rs — provider abstraction
- **`Llm` struct** (`llm.rs:38-51`): reqwest Client; one-shot sticky `auto_upgraded: AtomicBool` — a "use /v1/responses" provider error while `openai_api == Auto` permanently upgrades subsequent OpenAI calls to Responses API; `auth: Arc<dyn TokenSource>` — static bearer for OpenAI (OPENAI_COMPAT_API_KEY) and Databricks-with-token (DATABRICKS_TOKEN), refreshable PKCE engine otherwise; Anthropic bypasses this (x-api-key from cfg).
- **`complete()` dispatch** (`llm.rs:78-117+`): Provider::Anthropic → `anthropic_body` + `parse_anthropic`; OpenRouter → `openai_body` + `apply_openrouter_mutations` (thinking effort routing, prompt caching) + `post_openrouter` + `parse_openai_with_reasoning_details`; OpenAI/Databricks → chat or Responses bodies.
- **Body builders/parsers**: `anthropic_body` (:564) with rolling cache breakpoint stamping (:680) and anthropic cache_control application (:2521); `openai_body` (:710); `responses_body` (:843); `parse_anthropic` (:1364), `parse_openai` (:1438), `parse_responses` (:1007); tool-call ID dedup (:1350); usage summation across providers (:1160-1290).
- **Retry/timeouts**: MAX_RETRIES=3, base backoff 500ms → max 8s with jitter (:1615-1677); ESCALATION_TIMEOUT_CAP 1200s with escalated_timeout per consecutive timeout failures (:1626-1655); connect timeout 10s (`LLM_CONNECT_TIMEOUT` :58) with explicit NO client-level read timeout so per-request escalated budgets aren't floored (:62-68); retryable transport classification (:1679-1790); Retry-After header parsing capped at 60s (:2177-2197); 429/openrouter error classification (:2199-2236); context-length error detection triggers handoff (:946); unsupported-image-input detection (:1790).
- **Databricks**: OAuth scopes all-apis/offline_access, client databricks-cli (:20-21); v2 route mapping (:988-1005); PKCE flow lives in auth.rs (below).
- **Response size guards**: MAX_LLM_RESPONSE_BYTES 16MB, error body capped 4KB (:23-24); stall notice after 300s silence (:25).

### 2.2 agent.rs — session loop (LLM call → tool calls → MCP → repeat)
- **`RunCtx::run(prompt)`** (`agent.rs:303`): outer `loop` (:352) per turn: (1) handoff gate `maybe_handoff(&mut handoff_attempts)` (:364) — per-turn attempt counter scoped locally (:322-329) so long-lived sessions can handoff repeatedly; post-handoff threshold re-read prevention (:369); (2) LLM call with cancel/steer select arms (:397-432 — includes arm ensuring `should_handoff()` can't go permanently false); (3) capture input-token/history-bytes for the handoff gate (:486-540); (4) execute returned tool calls via MCP, emit wire updates (emit_pending/in_progress/completed/failed :1047-1097); (5) map stop reason (:1239).
- **Reply guard**: nags up to MAX_REPLY_NAGS=2 if turn ends without a `buzz messages send` call (`REPLY_GUARD_NAG` :82, detection `is_buzz_reply_call`/`is_reply_shaped` :99-122, server tag "buzz-agent" :75).
- **Resilience niceties**: unsupported-image stripping from history with explanatory message (:28, :40); MAX_TOKENS truncation recovery message instructing file-based incremental work (:34); synthetic hook outputs (:1136-1202); pricing-identity folding (:238).

### 2.3 mcp.rs — tool execution
- **Result budgets** (`mcp.rs:34-37`): total/text byte caps per tool result; schema caps: name ≤128, qualified name ≤64, ≤128 tools/session, description ≤1024B, schema ≤4096B, marker field ≤256B, hook result ≤16KB (:19-27); elision-marker allowance (:909).
- **Env sandboxing — PASSTHROUGH_ENV** (`mcp.rs:39-94`): MCP subprocesses run under `env_clear()` + explicit allowlist: core (PATH/HOME/TERM/LANG/LC_ALL/TMPDIR/XDG_CONFIG_HOME), SSH agent sockets, git helpers (GIT_ASKPASS/GIT_SSH_COMMAND/GIT_CONFIG_GLOBAL), both cases of proxy vars (comment: libcurl ignores uppercase HTTP_PROXY deliberately), TLS trust (SSL_CERT_FILE/DIR), **Buzz identity: NOSTR_PRIVATE_KEY, BUZZ_PRIVATE_KEY, BUZZ_RELAY_URL, BUZZ_AUTH_TAG** (comment :80-84: dev-mcp writes NOSTR_PRIVATE_KEY to keyfile then removes from own env; BUZZ_AUTH_TAG is non-secret ownership attestation; MCP subprocesses trusted like agent runtime), BUZZ_ACP_DISPLAY_NAME. Windows additions TMP/TEMP/USERPROFILE/APPDATA (+shell resolver keys) (:100-111). Test pins BUZZ_AUTH_TAG membership (:1041).
- **Registry**: `McpRegistry` (:189) manages servers; `spawn_one` (:738) applies passthrough env; restart state machine with max attempts (:162); transport-error-triggered restarts with capped jittered backoff (:833-857); fire-and-forget cancel (:818); process-group kill (:875-885); Windows no-window (:1025).

### 2.4 lib.rs — JSON-RPC server surface
- `dispatch` (:232) → `handle_request` (:245)/`handle_notification` (:288): initialize (:294, protocolVersion 2), session/new (:354, model catalog resolution :332 + configured-model fallback :346), session/cancel (:529), session/set_model (:545), steer (:596 — installs steer_tx, advertises activeRunId mirroring goose :701-711), session/prompt → `spawn_prompt` (:665) → **`run_prompt`** (:669-857): acquire_session lock-free-ish state pull (:689), RunCtx assembly (:730-754), ctx.run(), then writes history/task/handoff counters back and clears busy + active_run_id/steer_tx so late steers can't queue into finished turns (:756-766); usage notification emitted BEFORE the prompt response so buzz-acp's UsageTracker sees it in-flight (NIP-AM kind 44200 gating, :767-788); Databricks authenticate subcommand (:151, :161).

### 2.5 handoff.rs — history self-summarization
- Summarizer system prompt (`handoff.rs:44-50`): "context handoff summary for the next turn of an autonomous agent… plain text, no tool calls, no JSON… visible summary under <limit>"; empty-summary/summarize-error/cancellation outcomes are all bypassed so a flaky summarizer can't wedge the loop (:75-77).
- **Escalating rungs** (:116-174): if summarize fails or output still too big, LOOP with the next rung halving the summarizer's own prompt — the only way to converge; `HandoffOutcome` mechanism = summarize → reset history → re-seat live prompt (:174-217); handoff text injected as synthetic user message `[Context Handoff]\n{summary}` (:217-222); hook outputs folded in (:395-403).
- **Thresholds**: token estimate from bytes (:480-497), `token_threshold(max_context_tokens)` (:499) and byte-fallback threshold (:508) — handoff fires when estimated context tokens cross threshold OR bytes exceed fallback; per-run budget override (:439); uses `llm::summary_completion_cap`.

### 2.6 model_capabilities.rs
- Static manifest embedded from `scripts/model-capabilities.json` (`MANIFEST_JSON` include_str!, :225) validated at load (:399); `ThinkingMode`/`DatabricksV2Route`/`NormalizationPolicy` enums (:37-59); **`resolve(provider, raw_model_id) -> CapabilityResult`** (:288): canonicalizes provider (:243), strips catalog prefixes by family tokens (:255-277); Databricks known-model listing + registry labels (:373-383).

### 2.7 auth.rs — token sources
- Two engines behind `TokenSource`: `StaticTokenSource` (:77) and **`PkceOAuthTokenSource`** (:126-135) implementing RFC 6749 Authorization Code + RFC 7636 PKCE with on-disk refresh cache (private 0600 temp files, platform-specific creation :492-617, cache_path_for :447, expiry check :436); browser-mediated flow with local callback server that never outlives a failed attempt (:235-295, :427, `browser_pkce_flow` :718); pkce_pair = ~64-char URL-safe verifier + SHA-256 challenge (:657); random state (:668); headless guard: when no browser available returns Err(LlmAuth) instead of launching flow (:367).

---

## 3. buzz-cli — agent-first CLI

### 3.1 client.rs — REST wiring patterns (99KB)
- **`BuzzClient`** (`client.rs:521-531`): holds Keys, relay URL, reqwest client, optional auth-tag owner hex. Constructor `new` (:541); `sign_event` (:588) wraps EventBuilder signing; `with_auth_tag` (:616) attaches NIP-OA attestation header to requests; `auth_tag_owner_hex` (:576).
- **REST-only transport**: all I/O is HTTP bridge — `query`/`query_multi` POST filters (:767-803), `count` (:803), `get_authed` (:836), `submit_event` (:863), moderation/stored-event submits with ambiguity handling (:873-1073), ephemeral publish (:1073). `to_ws_url` exists only for display (:1299); no WebSocket client in the CLI. Pagination: QUERY_PAGE_SIZE=500 with cursor advance (:498-520), `query_paginated`/`query_all` (:715-743).
- **Retry pattern**: `with_retry_body` (:638) — RETRY_MAX_ATTEMPTS=3, base delays [0.5, 1.5]s, jitter, honors server `retry-in` hints parsed from body text (`parse_retry_hint_text` :155, `parse_retry_in_secs` :173, cap RETRY_IN_MAX_SECS=30 :130, env-overridable durations :140).
- **Media (Blossom)**: `upload_file` (:1100) — NIP-98 signed (`sign_nip98` :84) + Blossom upload auth (`sign_blossom_upload` :350); legacy-upload retry statuses narrow list (:200); ALLOWED_MIMES (:64), MAX_IMAGE_BYTES 50MB / MAX_VIDEO_BYTES 500MB (:73-76); imeta tag builder (:40). Download: server-scoped Blossom GET auth (:325), path-traversal defenses — sha256-lower-hex + safe extension checks (:250-270), same-relay media URL enforcement (tests :405-453).
- **Response normalization**: write contract `{event_id, accepted, message}` normalized via `normalize_write_response` (:1426), create-response id extraction (:1394-1413), sig-stripped event normalization for reads (`normalize_events` :1307), d-tag/p-tag extractors (:1325-1394).

### 3.2 validate.rs — content size limits
- **MAX_CONTENT_BYTES = 65_536 (64 KiB)** (`validate.rs:4`), enforced by `validate_content_size` (:64-73) → CliError::Usage (exit 1).
- **MAX_DIFF_BYTES = 61_440 (60 KiB)** (:7) with `truncate_diff` (:103-123): cuts at UTF-8 char boundary, prefers `\n@@` hunk boundary, falls back to last newline, appends `"[diff truncated — exceeded size limit]"`.
- Also: UUID/hex64/repo-id validators (:18-61; repo id `[a-zA-Z0-9._-]{1,64}`, no leading dot, no `..`), language inference from extension (:126-155), sdk_err mapping InvalidInput→Usage else Other exit 4 (:160-165), stdin `-` readers — `read_or_stdin` (value verbatim) vs `read_file_or_stdin` (always a path; regression test for the old bug where a missing path was returned as literal content, :186-198, test :499-505).

---

## 4. buzz-workflow — YAML-as-code workflow engine

Files: `buzz-workflow/src/{lib.rs (1910 ln, engine), executor.rs (1874), schema.rs, action_sink.rs, error.rs}`.

### 4.1 Trigger evaluation (lib.rs)
- **Trigger types** (`schema.rs:35-62`): internally-tagged enum — `message_posted` (optional evalexpr filter), `reaction_added` (optional emoji), `schedule` (cron XOR simple interval; validation :208-242 — cron required 7-field-normalized via `cron` crate :250-258, intervals must be ≥60s because the cron loop ticks every 60s :231-235), `webhook`, plus DiffPosted.
- **`should_fire_workflow()`** (`lib.rs:883-950`): ReactionAdded emoji mismatch → skip; MessagePosted/DiffPosted filter expr evaluated via `executor::evaluate_condition` against empty step outputs — Ok(false) skip, Err skip with warn (fail-closed).
- **`build_trigger_context()`** (`lib.rs:961-1021`): from StoredEvent — text=content, **author MUST come from event signature pubkey, never an `actor` tag** (signer-controlled metadata can't speak for another pubkey, :965-968); emoji = content for KIND_REACTION; message_id = last valid 64-hex `e` tag for reactions (NIP-25 direct target) else own id; channel_id/timestamp strings.
- **Authority**: `owner_authority_allows` (`lib.rs:1034-1040`): not-a-member ⇒ deny always; exfiltration-capable definitions (`requires_elevated_authority`) need owner/admin role; ordinary defs any member. Kind matching `trigger_matches_event` (:1043).

### 4.2 Cron scheduler (lib.rs)
- **Cron loop** (:473-750): 60s tick ("WorkflowEngine cron loop started (60s tick)" :490); window-based matching handles pod tick drift — `cron_fire_instant(expr, now, window_secs=60, wf_id)` (:765-794) finds the schedule instant inside the window and anchors on the CRON'S OWN scheduled time (stable across drift; tests :1059-1147 pin exact-boundary, drift-anchor, just-outside-none); interval triggers use bucket-based `interval_fire_instant`/`interval_should_fire`/prefilter (:796-861); fired workflows proceed with no trigger event (:672).

### 4.3 Condition evaluation (executor.rs)
- **`build_eval_context()`** (`executor.rs:224-315`): registers str_contains/str_starts_with/str_ends_with/str_len (evalexpr v11 lacks them); webhook fields registered FIRST as `trigger_<key>` but skipped if colliding with `trigger_*`/`steps_*` so standard fields can't be spoofed (:277-287); standard vars trigger_text/author/channel_id/timestamp/emoji/message_id (:289-301); step outputs exposed as `steps_<id>_output_<field>` (:303-312) via json_value_to_eval (:318).
- **`evaluate_condition()`** (:350+, EVAL_TIMEOUT=100ms :342): runs on blocking thread with hard tokio timeout — evalexpr "not designed for adversarial input" (:337-341).
- **Template resolution**: `resolve_template`/`resolve_variable` (:70-154) with filters (`apply_filter` :176); `resolve_step_templates` (:390) resolves {{var}} in step params before dispatch.

### 4.4 Step executor + approval suspension (executor.rs)
- **`execute_steps()`** (`executor.rs:1120-1250+`): sequential; supports resume via `start_index` + `initial_outputs` (approval-resume replays, also used by `execute_from_step` :1055); per step: optional `if:` condition (error ⇒ fail with PartialProgress{step_index, trace}), template resolution, dispatch under per-step timeout (`timeout_secs` or config default, :1173-1210 — timeout yields StepTimeout error); results recorded into trace + step_outputs.
- **Approval suspension**: `StepResult::Suspended { approval_token }` (:456, :1223-1236) — executor returns ExecutionResult{approval_token, step_index, step_outputs, trace} so the CALLER persists the approval record and updates the run trace; execution resumes later from step_index. Engine-side note (`lib.rs:229-254`): today the engine marks approval-gated runs Failed with code `approval_not_supported` ("see WF-08") — suspension plumbing exists end-to-end but resume is not yet wired in the engine path. Token generation stubbed (`generate_approval_token` :735).
- **`dispatch_action()`** (`executor.rs:519-735+`): BEFORE EVERY external side effect revalidates the durable community fence via `buzz_deletion::acquire_serving_write` + verify + protect (storage failure = denial, :529-547); SendMessage loads workflow run/workflow scoped to community (bare-id lookup could hit another community's row, :553-572), resolves destination channel (:468), publishes via action_sink with owner attribution (:587-596); AddReaction posts reaction on trigger.message_id (:611-636); CallWebhook with SSRF check (`check_ssrf` :782 — resolves host, blocks private IPs), shared reqwest client (:911), 1MB response cap (:815); SendDm/SetChannelTopic NotImplemented (WF-07 TODOs :599-609); `execute_run` entry (:1007).

---

## Cross-cutting observations

1. **Env-var credential chain is consistent**: harness (buzz-acp) injects BUZZ_RELAY_URL/BUZZ_PRIVATE_KEY/BUZZ_AUTH_TAG via session/new mcpServers[].env (lib.rs:5022-5077); buzz-cli consumes them via clap env bindings (config.rs:246-249); buzz-agent's MCP sandbox explicitly allowlists all three plus NOSTR_PRIVATE_KEY for grandchildren (mcp.rs:39-94). Empty BUZZ_AUTH_TAG is never forwarded (test lib.rs:6832-6839).
2. **Fail-closed philosophy repeats**: filter rules (filter.rs:357-367), workflow trigger filters (lib.rs:916-922), workflow write-fence (executor.rs:529-544), author-from-signature (lib.rs:965-968).
3. **Timeout layering**: WS ping/pong + auth timeouts (relay.rs) → idle+hard-deadline per turn with shared cancel budget (acp.rs) → LLM connect 10s + escalating request budgets capped 1200s (llm.rs) → 100ms evalexpr cap (both crates) → per-step workflow timeouts.
4. **Batching/dedup invariants**: single in-flight batch per channel, FIFO fairness by received_at, newest-last ordering fix for relay replay (queue.rs:346-350), withheld-steer parking preserves fairness, orphan auto-expiry past max_turn+100s.
5. **Not-yet-implemented seams**: workflow approval resume (WF-08), SendDm/SetChannelTopic (WF-07), CLI has REST-only transport despite to_ws_url helper.
